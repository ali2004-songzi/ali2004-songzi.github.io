# 内蒙古师范大学自然博物馆数字馆 - 使用说明

## 📁 项目结构

```
1122/
├── index.html              # 入口HTML
├── package.json            # 项目配置
├── vite.config.ts          # 构建工具配置
├── tailwind.config.js      # 样式配置
├── 启动网站.bat            # 一键启动脚本
├── 使用说明.md             # 本文档
├── public/
│   └── favicon.svg         # 网站图标
└── src/
    ├── main.tsx            # 程序入口
    ├── App.tsx             # 路由配置
    ├── index.css           # 全局样式
    ├── components/         # 公共组件
    │   ├── Header.tsx      # 顶部导航
    │   ├── Footer.tsx      # 页脚
    │   └── ui/             # UI组件库
    ├── data/               # 数据文件（重点！）
    │   ├── museum.ts       # 博物馆介绍
    │   ├── stats.ts        # 统计数据
    │   ├── collections.ts  # 三域馆藏
    │   ├── hologram.ts     # 全息标本数据
    │   └── taxonomy.ts     # 分类树 + 物种数据（最重要）
    ├── pages/              # 页面
    │   ├── HomePage/       # 首页
    │   ├── ExplorePage/    # 分类探索页
    │   ├── SpeciesDetailPage/  # 物种详情页
    │   ├── VrPage/         # VR展馆页
    │   └── NotFoundPage/   # 404页面
    └── lib/                # 工具函数
```

---

## 🚀 如何启动网站

### 方法一：一键启动（推荐）
直接双击 `启动网站.bat`，会自动：
1. 安装依赖（第一次运行时）
2. 启动开发服务器
3. 自动打开浏览器

### 方法二：手动启动
```bash
# 1. 安装依赖（第一次）
npm install

# 2. 启动开发服务器
npm run dev

# 3. 浏览器访问
# 打开 http://localhost:5173
```

### 停止网站
在命令行窗口按 `Ctrl + C`，然后按 `Y` 回车。

---

## ➕ 如何添加新物种

### 第一步：找到物种数据文件
打开 `src/data/taxonomy.ts` 文件

### 第二步：在 MOCK_SPECIES 数组中添加新物种

```typescript
export const MOCK_SPECIES = [
  // 已有物种...
  {
    id: 'sp-007',                    // 唯一ID，不能重复
    name: '新物种名称',               // 中文名称
    scientificName: 'Latin name',    // 拉丁学名
    rank: '种',                      // 分类阶元（界/门/纲/目/科/属/种）
    description: '物种描述...',       // 物种简介
    conservationStatus: 'LC',        // 保护等级（LC无危/NT近危/VU易危/EN濒危/CR极危）
    imageUrl: '图片URL',              // 物种图片
    category: 'animal',              // 类别（animal动物/plant植物）
    phylum: '脊索动物门',             // 门
    class: '哺乳纲',                  // 纲
    order: '食肉目',                  // 目
    family: '猫科',                   // 科
    genus: '豹属',                    // 属
    // 详细信息（物种详情页显示）
    morphology: '形态特征描述...',
    ecology: '生态作用描述...',
    habitat: '生活环境与分布描述...',
    habits: '生活习性描述...',
  },
  // 继续添加...
]
```

### 第三步：保存文件
保存后，网站会自动刷新（热更新），新物种就会出现在分类探索页面。

---

## 🎨 如何添加/修改3D模型

### 当前3D实现方式
目前的3D标本是用 **SVG + CSS 3D变换** 模拟的，不是真正的3D模型文件。

优点：
- 加载快，不需要额外的3D库
- 兼容性好
- 可以用代码控制

缺点：
- 不是真实的3D模型
- 只能展示简单的形状

### 方法一：修改现有的SVG 3D效果（简单）

打开对应的页面文件：
- 首页3D：`src/pages/HomePage/HologramSection.tsx`
- 详情页3D：`src/pages/SpeciesDetailPage/SpeciesDetailPage.tsx`

找到 SVG 部分的代码，修改形状：
```typescript
// 粒子模式 - 修改轮廓线
<ellipse cx="60" cy="60" rx="40" ry="35" />  // 椭圆
<circle cx="100" cy="75" r="30" />          // 圆形
<path d="M0 0 L100 100" />                  // 路径

// 实体模式 - 修改填充形状
<ellipse cx="60" cy="60" rx="40" ry="35" fill="#fbbf24" />
```

### 方法二：接入真实3D模型（复杂，需要开发）

如果要接入真实的 `.glb` / `.gltf` 3D模型文件，需要：

1. 安装Three.js库：
```bash
npm install three @react-three/fiber @react-three/drei
```

2. 创建3D模型组件：
```tsx
import { Canvas } from '@react-three/fiber'
import { GLTFLoader } from 'three/examples/jsm/loaders/GLTFLoader'

function Model({ url }) {
  // 加载3D模型
}
```

3. 把模型文件放到 `public/models/` 目录下

**建议**：如果需要真实3D模型，可以找我帮你升级。

---

## 🌳 如何修改分类树

打开 `src/data/taxonomy.ts` 文件，找到 `MOCK_TAXONOMY_TREE`：

```typescript
export const MOCK_TAXONOMY_TREE = {
  animal: {
    name: '动物界',
    scientific: 'Animalia',
    phyla: [
      {
        name: '脊索动物门',
        scientific: 'Chordata',
        classes: [
          // 添加纲...
          {
            name: '哺乳纲',
            scientific: 'Mammalia',
            orders: [
              // 添加目...
            ]
          }
        ]
      }
    ]
  },
  plant: {
    // 植物界...
  }
}
```

按照层级结构添加即可：界 → 门 → 纲 → 目 → 科 → 属 → 种

---

## 🖼️ 如何修改图片

### 方法一：直接替换图片URL
在数据文件中找到 `imageUrl` 字段，替换成新的图片链接即可。

### 方法二：使用本地图片
1. 把图片放到 `public/images/` 目录下
2. 在数据文件中引用：
```typescript
imageUrl: '/images/你的图片.jpg'
```

---

## 📝 如何修改首页内容

### 修改博物馆介绍
打开 `src/data/museum.ts`

### 修改统计数据
打开 `src/data/stats.ts`

### 修改三域馆藏
打开 `src/data/collections.ts`

### 修改全息标本
打开 `src/data/hologram.ts`

---

## 🎨 如何修改颜色/样式

### 修改主题颜色
打开 `tailwind.config.js`，找到 `colors` 部分：

```javascript
colors: {
  background: '#0a0e1a',      // 背景色
  primary: '#34d399',         // 主色调（青绿）
  secondary: '#fbbf24',       // 辅助色（琥珀）
  // 更多颜色...
}
```

### 修改全局样式
打开 `src/index.css`，可以添加自定义CSS。

---

## 🚀 如何打包发布

当网站做好后，需要打包成静态文件部署到服务器：

```bash
# 打包
npm run build

# 打包后的文件在 dist/ 目录下
# 把 dist/ 目录上传到服务器即可
```

### 本地预览打包结果
```bash
npm run preview
```

---

## ❓ 常见问题

### Q: 修改了数据文件但页面没更新？
A: 检查是否保存了文件，开发服务器会自动热更新。如果还是没更新，按 `F5` 刷新页面。

### Q: 图片不显示？
A: 检查图片URL是否正确，网络图片要确保可以访问。本地图片要放到 `public/` 目录下。

### Q: 启动报错？
A: 
1. 确保已经安装了 Node.js
2. 删除 `node_modules` 文件夹，重新运行 `npm install`
3. 看看报错信息是什么，发给我帮你解决

### Q: 怎么部署到网上？
A: 可以用以下平台：
- Vercel（免费，推荐）
- Netlify（免费）
- 阿里云/腾讯云服务器
- GitHub Pages

需要的话我可以教你怎么部署。

---

## 📞 需要帮助？

如果遇到问题，或者需要添加新功能，直接找我就行！

- 添加新物种/分类 ✅
- 修改页面内容 ✅
- 调整样式/颜色 ✅
- 添加新功能 ✅
- 部署上线 ✅
